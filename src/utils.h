#ifndef UTILS_H
#define UTILS_H

/**
 * @brief Prints error to standard out
 * @param errorCode error code corresponding to error message to print
 */
void printWindowsError(const long errorCode);

#endif // UTILS_H